[package]
name = "hotfix_patcher"
version = "0.1.0"
edition = "2021"

[dependencies]
app_lib = { package = "app", path = "../.." }
dirs = "6"
tempfile = "3"
walkdir = "2"
zip = { version = "2", default-features = false, features = ["deflate"] }
